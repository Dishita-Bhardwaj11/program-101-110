#include <stdio.h>
#include <math.h>

int main() {
    int n;
    scanf("%d", &n);

    double val = (double)n * (n + 1) / 2;
    double x = sqrt(val);

    if (x == (int)x)
        printf("%d", (int)x);
    else
        printf("-1");

    return 0;
}
